import * as React from 'react';

const Edit = () => {
    return (
        <div className="profile-user-settings">
            {/*<button className="btn profile-edit-btn">Edit Profile</button>*/}
            <button className="btn profile-edit-btn">Follows</button>
        </div>
);
};

export default Edit;

